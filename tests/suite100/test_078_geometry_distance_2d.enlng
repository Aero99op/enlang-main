type enlng
action dist2d(x1, y1, x2, y2):
    dx = x2 - x1
    dy = y2 - y1
    return sqrt(dx * dx + dy * dy)

d = dist2d(0, 0, 3, 4)
when d == 5:
    show "[TEST PASS 078] Geometry 2D Distance"
otherwise:
    show "[FAIL 078]"
